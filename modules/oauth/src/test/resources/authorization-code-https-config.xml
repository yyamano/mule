<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:oauth2="http://www.mulesoft.org/schema/mule/oauth2"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:tls="http://www.mulesoft.org/schema/mule/tls"
      xsi:schemaLocation="
       http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
       http://www.mulesoft.org/schema/mule/oauth2 http://www.mulesoft.org/schema/mule/oauth2/current/mule-oauth2.xsd
       http://www.mulesoft.org/schema/mule/tls http://www.mulesoft.org/schema/mule/tls/current/mule-tls.xsd
       http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    <http:request-config name="httpsRequestConfig">
        <!--<tls:context>-->
        <!--&lt;!&ndash;<tls:trust-store path="trustStore" password="mulepassword"/>&ndash;&gt;-->
        <!--<tls:key-store path="clientKeystore" keyPassword="mulepassword" password="mulepassword"/>-->
        <!--</tls:context>-->
        <tls:context>
            <!--<tls:trust-store path="ssltest-cacerts.jks" password="changeit"/>-->
            <!--<tls:key-store path="ssltest-keystore.jks" keyPassword="changeit" password="changeit"/>-->
        </tls:context>
    </http:request-config>

    <http:listener-config name="httpsListenerConfig">
        <tls:context>
            <!--<tls:trust-store path="ssltest-cacerts.jks" password="changeit"/>-->
            <tls:key-store path="ssltest-keystore.jks" keyPassword="changeit" password="changeit"/>
        </tls:context>
        <!--<tls:context>-->
        <!--<tls:trust-store path="trustStore" password="mulepassword"/>-->
        <!--<tls:key-store path="clientKeystore" keyPassword="mulepassword" password="mulepassword"/>-->
        <!--</tls:context>-->
    </http:listener-config>

    <oauth2:authentication-code name="minimalConfig"
                                clientId="${client.id}"
                                clientSecret="${client.secret}"
                                redirectionUrl="${redirect.url}"
                                requestConfig="httpsRequestConfig"
                                listenerConfig="httpsListenerConfig">
        <oauth2:authorization-request
                localAuthorizationUrl="${local.authorization.url}"
                authorizationUrl="${authorization.url}">
        </oauth2:authorization-request>
        <oauth2:token-request
                tokenUrl="${token.url}"/>
    </oauth2:authentication-code>

</mule>
